services:
  investclosure:
    build: .
    container_name: investclosure
    ports:
      - "5001:5001"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    restart: unless-stopped
